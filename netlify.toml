[build]
  command = "npm run build"
  publish = "dist"

# Branch deploy refresh: 2026-09-17
# akesq-v2 is the non-production review branch; production remains main.

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
